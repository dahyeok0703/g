// Stage 1 populates this with all domain types (WeaponSystem, ArmamentMount,
// Platform variants, Country, etc. — see spec §2). Kept as a placeholder so the
// folder structure and import path (`@/types`) exist from Stage 0.

export {};
